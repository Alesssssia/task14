version: '3.8'

services:
  apache:
    build: ./apache-folder
    container_name: apache-container
    restart: always

  nginx:
    build: ./nginx-folder
    container_name: nginx-container
    restart: always
    ports:
      - "80:8080"
      - "443:8443"
    depends_on:
      - apache
